# Copy to .env and fill in (never commit .env to GitHub)
# Linux/macOS: export $(grep -v '^#' .env | xargs)
# Windows PowerShell: Get-Content .env | ForEach-Object { ... } or set manually

TELEGRAM_TOKEN=
HEROSMS_API_KEY=
ALLOWED_USER_ID=
BALANCE_ALERT_LOW=2.0
